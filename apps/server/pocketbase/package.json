{
  "name": "fw-roedingen-pocketbase",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc && cp src/main.go dist/src/main.go && cp src/go.sum dist/src/go.sum && cp src/go.mod dist/src/go.mod && cp -r src/pb_data dist/src/pb_data"
  },
  "dependencies": {
    "cross-fetch": "^3.1.5",
    "eventsource": "^2.0.2",
    "pocketbase": "^0.10.1"
  }
}
