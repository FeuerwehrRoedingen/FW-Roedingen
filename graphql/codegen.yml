overwrite: true
schema: "schema.graphql"
generates:
  ./src/__generated__/resolvers.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      contextType: "../context#FWR_Context"
  ./graphql.schema.json:
    plugins:
      - "introspection"
